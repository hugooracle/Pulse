# Pulse — upstream

Pulse is derived from SimpMusic and remains subject to the GPL-3.0 terms applicable to the upstream code.

Upstream repository: `maxrave-dev/SimpMusic`
Upstream branch: `dev`
Pulse repository: `hugooracle/Pulse`

The internal Kotlin namespace is intentionally kept as `com.maxrave.simpmusic` during the first rebranding pass to reduce regression risk. Public-facing identifiers are migrated first.
